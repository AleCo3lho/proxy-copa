# proxy-copa — personal WireGuard VPN on AWS

Dependencies:
aws-cli
aws ssm client
wireguard

Need a shell with the AWS credentials to be used configure, run ./scripts/matchday.sh up to spin up and configure the vpn.

Use ./scripts/matchday.sh down to destroy
